# Copy **.env.example** to **.env**
```bash
cp .env.example .env
```

# Modify the necessary variables as you want
```
vi .env
```
# Start the Services
```bash
docker compose pull && docker compose up -d
```
